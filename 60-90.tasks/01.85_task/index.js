// Дана некоторая переменная с числом. Надо сделать строку, содержащую столько нулей, сколько указано в переменной.

let num = 5;
let zeroStr = "0".repeat(num);

console.log(zeroStr);

let name = "Zhakyp ";

let zeroName = name.repeat(10);

console.log(zeroName);
